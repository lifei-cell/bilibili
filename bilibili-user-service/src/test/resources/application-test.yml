spring:
  cloud:
    discovery:
      enabled: false
    nacos:
      discovery:
        enabled: false
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: false

logging:
  level:
    com.alibaba.cloud.nacos: OFF
