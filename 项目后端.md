# 项目后端文档

## 一、项目概述

本项目是一个基于 **SpringBoot + Vue3** 的视频开发平台后端服务，支持用户管理、文件上传与断点续传、实时互动、高效搜索等核心功能。通过分层解耦和中间件集成，实现高性能、高可用性及可扩展性。

### 技术栈

| 分类 | 技术 | 说明 |
|------|------|------|
| 基础框架 | SpringBoot 4.x / SpringCloud | 微服务基础架构 |
| 服务治理 | Nacos | 服务注册、发现与配置中心 |
| 鉴权 | Sa-Token | 轻量级权限认证框架 |
| 数据库 | MySQL 8.x | 关系型数据存储 |
| 缓存 | Redis 7.x | 热点数据缓存、分布式锁、Lua 限流 |
| 消息队列 | RocketMQ | 异步解耦、削峰填谷 |
| 搜索引擎 | ElasticSearch 7.x | 全文检索与聚合分析 |
| 对象存储 | MinIO | 视频/图片文件存储 |
| 数据同步 | Canal | MySQL Binlog 监听，同步到 ES/Redis |
| 实时通信 | Netty / WebSocket | 弹幕长连接、实时互动 |
| 反向代理 | Nginx | 负载均衡、静态资源、限流 |
| 容器化 | Docker | 容器化部署 |

---

## 二、项目架构

### 2.1 分层架构

```
┌─────────────────────────────────────────────────────────────┐
│                    Nginx 反向代理层                           │
│              (负载均衡 / 静态资源 / 限流)                      │
└─────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────────────────────────────────────┐
│                    Gateway 网关层                            │
│              (路由转发 / 统一鉴权 / 限流)                      │
└─────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────────────────────────────────────┐
│                    业务服务层                                │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐       │
│  │ 用户服务  │ │ 视频服务  │ │ 弹幕服务  │ │ 搜索服务  │       │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘       │
└─────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────────────────────────────────────┐
│                    中间件层                                  │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐    │
│  │MySQL │ │Redis │ │  MQ  │ │  ES  │ │MinIO │ │Canal │    │
│  └──────┘ └──────┘ └──────┘ └──────┘ └──────┘ └──────┘    │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 项目包结构

```
gary.bilibili
├── BilibiliApplication.java          # 启动类
├── common/                            # 公共模块
│   ├── config/                        # 通用配置
│   │   ├── RedisConfig.java           # Redis 配置
│   │   ├── MinIOConfig.java           # MinIO 配置
│   │   ├── RocketMQConfig.java        # RocketMQ 配置
│   │   ├── ESConfig.java              # ElasticSearch 配置
│   │   ├── WebMvcConfig.java          # Web MVC 配置 (CORS / 拦截器)
│   │   └── SaTokenConfig.java         # Sa-Token 配置
│   ├── exception/                     # 全局异常处理
│   │   ├── GlobalExceptionHandler.java
│   │   ├── BusinessException.java
│   │   └── ErrorCode.java             # 错误码枚举
│   ├── result/                        # 统一响应
│   │   └── Result.java                # 统一返回体
│   ├── constant/                      # 常量
│   │   ├── RedisConstant.java
│   │   └── MQConstant.java
│   ├── util/                          # 工具类
│   │   ├── FileUtil.java
│   │   ├── HashUtil.java
│   │   └── IpUtil.java
│   └── annotation/                    # 自定义注解
│       └── RateLimit.java             # 限流注解
├── module-user/                       # 用户模块
│   ├── controller/
│   │   └── UserController.java
│   ├── service/
│   │   ├── UserService.java
│   │   └── impl/UserServiceImpl.java
│   ├── mapper/
│   │   └── UserMapper.java
│   ├── entity/
│   │   ├── User.java
│   │   └── UserAuth.java
│   ├── dto/
│   │   ├── LoginDTO.java
│   │   ├── RegisterDTO.java
│   │   └── UserInfoDTO.java
│   └── constant/
│       └── UserConstant.java
├── module-video/                      # 视频模块
│   ├── controller/
│   │   ├── VideoController.java       # 视频 CRUD
│   │   └── FileController.java        # 文件上传
│   ├── service/
│   │   ├── VideoService.java
│   │   ├── FileService.java
│   │   └── impl/
│   ├── mapper/
│   │   └── VideoMapper.java
│   ├── entity/
│   │   ├── Video.java
│   │   ├── VideoStats.java
│   │   └── FileChunk.java             # 文件分片记录
│   └── dto/
│       ├── VideoDTO.java
│       └── UploadResultDTO.java
├── module-danmu/                      # 弹幕模块
│   ├── server/
│   │   ├── DanmuNettyServer.java      # Netty 服务端启动
│   │   └── DanmuWebSocketServer.java  # WebSocket 处理器
│   ├── handler/
│   │   ├── DanmuChannelHandler.java   # 管道处理器
│   │   └── HeartBeatHandler.java      # 心跳检测
│   ├── service/
│   │   ├── DanmuService.java
│   │   └── impl/DanmuServiceImpl.java
│   └── entity/
│       └── Danmu.java
├── module-search/                     # 搜索模块
│   ├── controller/
│   │   └── SearchController.java
│   ├── service/
│   │   ├── SearchService.java
│   │   └── impl/SearchServiceImpl.java
│   └── repository/
│       └── VideoRepository.java       # ES Repository
├── module-social/                     # 社交互动模块
│   ├── controller/
│   │   ├── FollowController.java
│   │   ├── LikeController.java
│   │   ├── CommentController.java
│   │   └── CollectionController.java
│   ├── service/
│   │   └── impl/
│   └── entity/
│       ├── Follow.java
│       ├── Like.java
│       ├── Comment.java
│       └── Collection.java
└── module-canal/                      # Canal 数据同步模块
    ├── handler/
    │   └── CanalDataHandler.java      # Binlog 数据处理器
    └── client/
        └── CanalClient.java           # Canal 客户端
```

---

## 三、数据库设计

### 3.1 用户主表 (sys_user)

> 存放用户核心信息，写少读多，字段相对稳定。

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键，自增 |
| username | VARCHAR(50) | 用户名，唯一 |
| password | VARCHAR(255) | BCrypt 加密密码 |
| phone | VARCHAR(20) | 手机号 |
| avatar | VARCHAR(500) | 头像 URL |
| gender | TINYINT | 性别 (0未知 1男 2女) |
| birthday | DATE | 生日 |
| signature | VARCHAR(200) | 个性签名 |
| status | TINYINT | 状态 (0正常 1封禁) |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |
| deleted | TINYINT | 逻辑删除 (0否 1是) |



### 3.3 视频主表 (video)

> 存放视频属性信息，上传后基本不变，写少读多。

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键，自增 |
| user_id | BIGINT | 上传用户 ID |
| title | VARCHAR(200) | 视频标题 |
| description | TEXT | 视频简介 |
| cover_url | VARCHAR(500) | 封面图 URL |
| video_url | VARCHAR(500) | 视频文件 URL |
| duration | INT | 视频时长(秒) |
| file_size | BIGINT | 文件大小(字节) |
| file_md5 | VARCHAR(64) | 文件 MD5 (秒传校验) |
| resolution | VARCHAR(20) | 分辨率 (1080P/4K等) |
| category_id | INT | 分类 ID |
| tags | VARCHAR(500) | 标签，逗号分隔 |
| status | TINYINT | 状态 (0审核中 1通过 2不通过 3下架) |
| audit_remark | VARCHAR(500) | 审核备注 |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |
| deleted | TINYINT | 逻辑删除 |

### 3.4 视频统计表 (video_stats)

> 存放视频互动计数，每次点赞/投币/播放都会更新，高频写入，与 `video` 一对一关联。

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键，自增 |
| video_id | BIGINT | 视频 ID，唯一索引 |
| view_count | BIGINT | 播放量 |
| like_count | BIGINT | 点赞数 |
| coin_count | BIGINT | 投币数 |
| collect_count | BIGINT | 收藏数 |
| share_count | BIGINT | 分享数 |
| danmu_count | BIGINT | 弹幕数 |
| comment_count | BIGINT | 评论数 |
| update_time | DATETIME | 更新时间 |

### 3.5 文件分片表 (file_chunk)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键，自增 |
| file_md5 | VARCHAR(64) | 文件完整 MD5 |
| chunk_md5 | VARCHAR(64) | 分片 MD5 |
| chunk_index | INT | 分片序号 (从0开始) |
| chunk_size | BIGINT | 分片大小(字节) |
| total_chunks | INT | 总分片数 |
| file_name | VARCHAR(255) | 原始文件名 |
| upload_id | VARCHAR(64) | 上传任务 ID |
| status | TINYINT | 状态 (0上传中 1完成) |
| expire_time | DATETIME | 分片过期时间 |
| create_time | DATETIME | 创建时间 |

### 3.6 弹幕表 (danmu)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键，自增 |
| user_id | BIGINT | 发送用户 ID |
| video_id | BIGINT | 关联视频 ID |
| content | VARCHAR(200) | 弹幕内容 |
| color | VARCHAR(10) | 弹幕颜色 (16进制) |
| position | TINYINT | 弹幕位置 (0滚动 1顶部 2底部) |
| font_size | INT | 字体大小 |
| video_time | INT | 视频时间点(秒) |
| send_time | DATETIME | 发送时间 |

### 3.7 评论表 (comment)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键，自增 |
| user_id | BIGINT | 评论用户 ID |
| video_id | BIGINT | 关联视频 ID |
| parent_id | BIGINT | 父评论 ID (0表示一级评论) |
| reply_to_id | BIGINT | 回复目标用户 ID |
| content | TEXT | 评论内容 |
| like_count | INT | 点赞数 |
| create_time | DATETIME | 创建时间 |
| deleted | TINYINT | 逻辑删除 |

### 3.8 关注表 (follow)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键，自增 |
| follower_id | BIGINT | 关注者用户 ID |
| followed_id | BIGINT | 被关注者用户 ID |
| status | TINYINT | 状态 (1关注中 0已取消) |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |

### 3.9 点赞表 (`like`)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键，自增 |
| user_id | BIGINT | 用户 ID |
| target_type | TINYINT | 目标类型 (1视频 2评论) |
| target_id | BIGINT | 目标 ID |
| status | TINYINT | 状态 (1点赞 0取消) |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |

### 3.10 收藏表 (collection)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键，自增 |
| user_id | BIGINT | 用户 ID |
| video_id | BIGINT | 视频 ID |
| folder_id | BIGINT | 收藏夹 ID (默认0) |
| status | TINYINT | 状态 (1收藏中 0已移除) |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |

### 3.11 收藏夹表 (collection_folder)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键，自增 |
| user_id | BIGINT | 用户 ID |
| name | VARCHAR(50) | 收藏夹名称 |
| is_public | TINYINT | 是否公开 (0私密 1公开) |
| description | VARCHAR(200) | 收藏夹描述 |
| cover_url | VARCHAR(500) | 收藏夹封面 |
| video_count | INT | 视频数量(冗余) |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |

---

## 四、接口文档

### 4.1 统一返回格式

```json
{
  "code": 200,
  "message": "success",
  "data": {},
  "timestamp": 1716420000000
}
```

> **约定**: `code` 使用 HTTP 状态码风格，`200` 成功，`4xx` 客户端错误，`5xx` 服务端错误。

---

### 4.2 用户模块接口

#### 4.2.1 用户注册

| 项目 | 内容 |
|------|------|
| **接口路径** | `POST /api/user/register` |
| **请求参数** | Body (JSON) |

```json
{
  "username": "zhangsan",
  "password": "Abc@123456",
  "email": "zhangsan@example.com",
  "phone": "13800138000",
  "captcha": "a3f2"
}
```

| **返回数据** | 用户基本信息 (id, username, token) |

#### 4.2.2 用户登录

| 项目 | 内容 |
|------|------|
| **接口路径** | `POST /api/user/login` |
| **请求参数** | Body (JSON) |

```json
{
  "username": "zhangsan",
  "password": "Abc@123456",
  "captcha": "a3f2"
}
```

| **返回数据** | Sa-Token 生成的 token 字符串 + 用户信息 |

> **同端登录检测**: 后登录的设备会使同类型终端(Web/App)上的旧 token 失效，通过 Redis 存储 `userId -> token` 映射实现。

#### 4.2.3 验证码获取

| 项目 | 内容 |
|------|------|
| **接口路径** | `GET /api/user/captcha` |
| **请求参数** | 无 |
| **返回数据** | `{ "captchaId": "uuid", "captchaImg": "base64..." }` |

#### 4.2.4 退出登录

| 项目 | 内容 |
|------|------|
| **接口路径** | `POST /api/user/logout` |
| **鉴权** | 需要登录 |
| **返回数据** | 空 |

#### 4.2.5 获取当前用户信息

| 项目 | 内容 |
|------|------|
| **接口路径** | `GET /api/user/info` |
| **鉴权** | 需要登录 |
| **返回数据** | 用户详细信息 (UserInfoDTO) |

#### 4.2.6 修改用户资料

| 项目 | 内容 |
|------|------|
| **接口路径** | `PUT /api/user/info` |
| **鉴权** | 需要登录 |
| **请求参数** | Body (JSON) |

```json
{
  "nickname": "张三",
  "avatar": "https://minio.example.com/avatars/xxx.png",
  "gender": 1,
  "birthday": "2000-01-01",
  "signature": "这个人很懒，什么都没写"
}
```

#### 4.2.7 修改密码

| 项目 | 内容 |
|------|------|
| **接口路径** | `PUT /api/user/password` |
| **鉴权** | 需要登录 |
| **请求参数** | Body (JSON) |

```json
{
  "oldPassword": "Abc@123456",
  "newPassword": "Xyz@654321"
}
```

#### 4.2.8 获取用户主页信息

| 项目 | 内容 |
|------|------|
| **接口路径** | `GET /api/user/profile/{userId}` |
| **鉴权** | 无需登录 |
| **返回数据** | 用户公开信息 + 视频数量 + 粉丝数 + 关注数 |

---

### 4.3 视频模块接口

#### 4.3.1 文件分片上传

| 项目 | 内容 |
|------|------|
| **接口路径** | `POST /api/file/chunk-upload` |
| **鉴权** | 需要登录 |
| **请求参数** | multipart/form-data |

| 参数 | 类型 | 说明 |
|------|------|------|
| file | File | 分片文件 |
| md5 | String | 文件完整 MD5 |
| chunkMd5 | String | 分片 MD5 |
| chunkIndex | Integer | 分片序号 (0 起始) |
| totalChunks | Integer | 总分片数 |
| fileName | String | 原始文件名 |
| fileSize | Long | 文件总大小 |

| **返回数据** | `{ "uploadId": "xxx", "chunkIndex": 0, "uploaded": true }` |

> **分片策略**: 每片 5MB，客户端切分后逐个上传，后端将分片存入 MinIO 临时目录。

#### 4.3.2 秒传检测

| 项目 | 内容 |
|------|------|
| **接口路径** | `POST /api/file/check-md5` |
| **鉴权** | 需要登录 |
| **请求参数** | Body (JSON) |

```json
{
  "md5": "d41d8cd98f00b204e9800998ecf8427e",
  "fileName": "video.mp4"
}
```

| **返回数据** | `{ "exist": true, "videoId": 12345 }` 或 `{ "exist": false, "uploadedChunks": [0, 1, 3] }` |

> `uploadedChunks` 返回已上传的分片序号数组，供客户端实现断点续传。

#### 4.3.3 分片合并请求

| 项目 | 内容 |
|------|------|
| **接口路径** | `POST /api/file/merge-chunks` |
| **鉴权** | 需要登录 |
| **请求参数** | Body (JSON) |

```json
{
  "md5": "d41d8cd98f00b204e9800998ecf8427e",
  "fileName": "video.mp4",
  "totalChunks": 10
}
```

| **返回数据** | `{ "videoUrl": "https://...", "videoId": 12345 }` |

> 合并后通过 RocketMQ 异步触发视频转码任务。

#### 4.3.4 获取上传进度

| 项目 | 内容 |
|------|------|
| **接口路径** | `GET /api/file/upload-progress/{md5}` |
| **鉴权** | 需要登录 |
| **返回数据** | `{ "uploadedChunks": [0,1,2], "totalChunks": 10, "percent": 30 }` |

#### 4.3.5 发布视频

| 项目 | 内容 |
|------|------|
| **接口路径** | `POST /api/video/publish` |
| **鉴权** | 需要登录 |
| **请求参数** | Body (JSON) |

```json
{
  "title": "视频标题",
  "description": "视频简介",
  "coverUrl": "https://minio.example.com/covers/xxx.png",
  "categoryId": 1,
  "tags": ["Java", "SpringBoot"],
  "sourceUrl": "https://minio.example.com/videos/xxx.mp4",
  "duration": 360,
  "fileSize": 102400000,
  "fileMd5": "d41d8cd98f00b204e9800998ecf8427e",
  "resolution": "1080P"
}
```

| **返回数据** | `{ "videoId": 12345 }` |

#### 4.3.6 视频详情

| 项目 | 内容 |
|------|------|
| **接口路径** | `GET /api/video/{videoId}` |
| **鉴权** | 无需登录 |
| **返回数据** | 视频详情 + 作者信息 + 互动数据 |

> 播放量通过 Redis 异步累加后，定时同步到 MySQL。

#### 4.3.7 视频列表（推荐/热门/分类）

| 项目 | 内容 |
|------|------|
| **接口路径** | `GET /api/video/list` |
| **请求参数** | Query |

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| page | Integer | 否 | 页码 (默认 1) |
| size | Integer | 否 | 每页数量 (默认 20) |
| categoryId | Integer | 否 | 分类筛选 |
| sort | String | 否 | 排序: `hot`(热门) / `new`(最新) / `default`(推荐) |

| **返回数据** | 分页视频列表 |

#### 4.3.8 用户发布的视频列表

| 项目 | 内容 |
|------|------|
| **接口路径** | `GET /api/video/user/{userId}` |
| **请求参数** | Query: page, size |
| **返回数据** | 分页视频列表 |

#### 4.3.9 修改视频信息

| 项目 | 内容 |
|------|------|
| **接口路径** | `PUT /api/video/{videoId}` |
| **鉴权** | 需要登录 (仅作者或管理员) |
| **请求参数** | Body (JSON): 可编辑字段 |

#### 4.3.10 删除视频

| 项目 | 内容 |
|------|------|
| **接口路径** | `DELETE /api/video/{videoId}` |
| **鉴权** | 需要登录 (仅作者或管理员) |
| **返回数据** | 空 |

---

### 4.4 弹幕模块接口

#### 4.4.1 WebSocket 弹幕连接

| 项目 | 内容 |
|------|------|
| **连接地址** | `ws://host:port/api/danmu/ws/{videoId}` |
| **协议** | WebSocket over Netty |
| **鉴权** | 登录用户先申请一次性 Ticket，并通过 `ticket` 参数连接；Access Token 不进入 URL |

**推送消息格式** (服务端 → 客户端):

```json
{
  "type": "danmu",
  "data": {
    "id": 1001,
    "userId": 12345,
    "username": "zhangsan",
    "avatar": "https://...",
    "content": "哈哈哈",
    "color": "#FFFFFF",
    "position": 0,
    "fontSize": 16,
    "videoTime": 120
  }
}
```

**心跳消息**:

```json
{ "type": "heartbeat" }
```

#### 4.4.2 发送弹幕 (HTTP 接口)

| 项目 | 内容 |
|------|------|
| **接口路径** | `POST /api/danmu/send` |
| **鉴权** | 需要登录 |
| **请求参数** | Body (JSON) |

```json
{
  "videoId": 12345,
  "content": "哈哈哈",
  "color": "#FFFFFF",
  "position": 0,
  "fontSize": 16,
  "videoTime": 120
}
```

> 后端通过 RocketMQ 异步写入 MySQL，同时广播到 Netty 连接池推送给同视频的所有在线用户。

#### 4.4.3 获取视频弹幕列表

| 项目 | 内容 |
|------|------|
| **接口路径** | `GET /api/danmu/list/{videoId}` |
| **请求参数** | Query: videoTime(时间点), page, size (默认 500 条/页) |
| **返回数据** | 按 videoTime 排序的弹幕列表 |

#### 4.4.4 弹幕数量统计

| 项目 | 内容 |
|------|------|
| **接口路径** | `GET /api/danmu/count/{videoId}` |
| **返回数据** | `{ "total": 10000, "timeDistributed": [{"time": 0, "count": 50}, ...] }` |

---

### 4.5 搜索模块接口

#### 4.5.1 综合搜索

| 项目 | 内容 |
|------|------|
| **接口路径** | `GET /api/search/query` |
| **请求参数** | Query |

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| keyword | String | 是 | 搜索关键词 |
| type | String | 否 | 类型: `video` / `user` / `all` (默认 all) |
| categoryId | Integer | 否 | 分类筛选 |
| sort | String | 否 | `hot` / `new` / `default` |
| page | Integer | 否 | 页码 |
| size | Integer | 否 | 每页数量 |
| minDuration | Integer | 否 | 最小时长(秒) |
| maxDuration | Integer | 否 | 最大时长(秒) |

| **返回数据** | 分页搜索结果 (高亮标题 + 聚合统计) |

#### 4.5.2 搜索建议（自动补全）

| 项目 | 内容 |
|------|------|
| **接口路径** | `GET /api/search/suggest` |
| **请求参数** | Query: keyword, size(默认10) |
| **返回数据** | `["Java入门", "Java面试", "Java项目实战"]` |

> 基于 ES Completion Suggester 实现，搜索记录通过 Canal 增量同步到 ES。

#### 4.5.3 热搜榜

| 项目 | 内容 |
|------|------|
| **接口路径** | `GET /api/search/hot` |
| **请求参数** | Query: size(默认20) |
| **返回数据** | 热搜关键词列表 (Redis Sorted Set 维护搜索频率) |

---

### 4.6 社交互动模块接口

#### 4.6.1 关注用户

| 项目 | 内容 |
|------|------|
| **接口路径** | `POST /api/follow/follow/{userId}` |
| **鉴权** | 需要登录 |
| **返回数据** | `{ "followed": true }` |

#### 4.6.2 取消关注

| 项目 | 内容 |
|------|------|
| **接口路径** | `DELETE /api/follow/follow/{userId}` |
| **鉴权** | 需要登录 |

#### 4.6.3 获取关注列表

| 项目 | 内容 |
|------|------|
| **接口路径** | `GET /api/follow/following/{userId}` |
| **请求参数** | Query: page, size |
| **返回数据** | 分页关注用户列表 |

#### 4.6.4 获取粉丝列表

| 项目 | 内容 |
|------|------|
| **接口路径** | `GET /api/follow/follower/{userId}` |
| **请求参数** | Query: page, size |
| **返回数据** | 分页粉丝列表 |

#### 4.6.5 关注状态查询

| 项目 | 内容 |
|------|------|
| **接口路径** | `GET /api/follow/status/{userId}` |
| **鉴权** | 需要登录 |
| **返回数据** | `{ "isFollowing": true }` |

#### 4.6.6 点赞

| 项目 | 内容 |
|------|------|
| **接口路径** | `POST /api/like/like` |
| **鉴权** | 需要登录 |
| **请求参数** | Body (JSON) |

```json
{
  "targetType": 1,
  "targetId": 12345
}
```

#### 4.6.7 取消点赞

| 项目 | 内容 |
|------|------|
| **接口路径** | `DELETE /api/like/like` |
| **鉴权** | 需要登录 |
| **请求参数** | Body (JSON): 同上 |

> 点赞数据通过 Redis Set 维护 `like:type:targetId`，使用 Canal 监听 MySQL Binlog 保证 Redis 缓存一致性。

#### 4.6.8 发布评论

| 项目 | 内容 |
|------|------|
| **接口路径** | `POST /api/comment/publish` |
| **鉴权** | 需要登录 |
| **请求参数** | Body (JSON) |

```json
{
  "videoId": 12345,
  "content": "评论内容",
  "parentId": 0,
  "replyToId": 0
}
```

#### 4.6.9 评论列表

| 项目 | 内容 |
|------|------|
| **接口路径** | `GET /api/comment/list/{videoId}` |
| **请求参数** | Query: page, size, sort(`hot`/`new`) |
| **返回数据** | 分页评论列表 (含二级回复) |

#### 4.6.10 删除评论

| 项目 | 内容 |
|------|------|
| **接口路径** | `DELETE /api/comment/{commentId}` |
| **鉴权** | 需要登录 (仅作者或视频作者) |

#### 4.6.11 添加收藏

| 项目 | 内容 |
|------|------|
| **接口路径** | `POST /api/collection/collect` |
| **鉴权** | 需要登录 |
| **请求参数** | Body (JSON): `{ "videoId": 12345, "folderId": 0 }` |

#### 4.6.12 取消收藏

| 项目 | 内容 |
|------|------|
| **接口路径** | `DELETE /api/collection/collect` |
| **鉴权** | 需要登录 |
| **请求参数** | Body (JSON): `{ "videoId": 12345 }` |

#### 4.6.13 收藏列表

| 项目 | 内容 |
|------|------|
| **接口路径** | `GET /api/collection/list` |
| **鉴权** | 需要登录 |
| **请求参数** | Query: folderId(可选), page, size |
| **返回数据** | 分页收藏视频列表 |

#### 4.6.14 创建/编辑收藏夹

| 项目 | 内容 |
|------|------|
| **接口路径** | `POST /api/collection/folder` (创建) / `PUT /api/collection/folder/{folderId}` (编辑) |
| **鉴权** | 需要登录 |
| **请求参数** | Body (JSON): `{ "name": "学习资料", "isPublic": true, "description": "..." }` |

#### 4.6.15 我的收藏夹列表

| 项目 | 内容 |
|------|------|
| **接口路径** | `GET /api/collection/folders` |
| **鉴权** | 需要登录 |
| **返回数据** | 用户所有收藏夹 |

---

### 4.7 视频分类接口

#### 4.7.1 获取全部分类

| 项目 | 内容 |
|------|------|
| **接口路径** | `GET /api/category/list` |
| **返回数据** | 已启用分类列表，包含 `id`、`parentId`、`name`、`sort` |

---

## 五、关键技术方案

### 5.1 文件断点续传与秒传

```
┌──────────┐    ┌──────────┐    ┌──────────┐
│  客户端   │    │  后端服务  │    │  MinIO   │
└────┬─────┘    └────┬─────┘    └────┬─────┘
     │               │               │
     │ 1.POST /check-md5              │
     │──────────────>│               │
     │  返回已上传分片索引             │
     │<──────────────│               │
     │               │               │
     │ 2.POST /chunk-upload (循环)    │
     │──────────────>│               │
     │               │ 存储分片      │
     │               │──────────────>│
     │  返回上传结果  │               │
     │<──────────────│               │
     │               │               │
     │ 3.POST /merge-chunks          │
     │──────────────>│               │
     │               │ 合并分片      │
     │               │──────────────>│
     │               │ MQ: 转码任务  │
     │  返回视频URL  │               │
     │<──────────────│               │
```

- **秒传**: 上传前先传文件 MD5，若已存在相同 MD5 的视频则直接返回已有视频 ID
- **断点续传**: 检测已上传分片，跳过已有分片，仅上传缺失分片
- **分片合并**: 通过 MinIO Compose API 在服务端合并分片，避免本地 I/O
- **转码**: RocketMQ 异步触发 FFmpeg 转码任务，生成浏览器兼容的 MP4；后续可在同一 worker 中扩展多清晰度

### 5.2 Netty 弹幕长连接

- **连接架构**: Netty 作为独立服务运行，使用 `EventLoopGroup` 管理线程模型
- **心跳机制**: IdleStateHandler 检测空闲连接(读空闲 60s)，超时自动断开
- **连接管理**: ConcurrentHashMap 管理 `videoId -> ChannelGroup` 映射，组播弹幕消息
- **千万级连接**: 单机 Netty + Linux 内核参数调优(tcp_mem, somaxconn, 文件描述符上限)，配合 Nginx 四层负载实现水平扩展
- **消息流程**: 
  1. 用户发送弹幕 → HTTP API → 写入 MQ
  2. MQ Consumer → 写入 MySQL → 查询同视频在线用户
  3. Netty → 组播推送弹幕消息到所有在线观众

### 5.3 Redis 缓存与一致性

- **缓存策略**:
  - 视频详情: `video:detail:{videoId}` → JSON（video + video_stats 聚合），TTL 30 分钟
  - 视频统计: `video:stats:{videoId}` → Hash，实时更新各计数，定时同步 MySQL
  - 用户信息: `user:info:{userId}` → JSON，TTL 1 小时（读取 sys_user）
  - 用户登录态: `user:auth:{userId}` → JSON，TTL 30 分钟（读取 sys_user_auth，同端登录检测）
  - 点赞状态: `like:video:{videoId}` → Set(userIds)
  - 关注关系: `follow:follower:{userId}` → Set(followedUserIds)

- **缓存一致性 (Canal + MQ 方案)**:
  1. Canal 监听 MySQL Binlog
  2. 数据变更 → Canal 发送到 RocketMQ
  3. 消费者解析变更 → 更新/删除对应 Redis 缓存
  4. 同时同步到 ElasticSearch 更新索引

- **缓存防穿透 (布隆过滤器)**:
  - 启动时加载所有视频 ID 到布隆过滤器
  - 查询前先过布隆过滤器，不存在的 ID 直接返回空
  - Canal 监听新增视频 → 同步追加到布隆过滤器

### 5.4 Lua 限流

- **注解式限流**: 通过 `@RateLimit(key, count, period)` 注解声明限流规则
- **Redis Lua 脚本**: 滑动窗口算法，原子性计数
- **限流场景**:
  - 登录接口: `login:{ip}` → 10次/分钟
  - 发弹幕: `danmu:{userId}` → 20次/分钟
  - 视频上传: `upload:{userId}` → 5次/小时
  - 全局接口: `global:{path}` → 1000次/秒

```lua
-- 滑动窗口限流 Lua 脚本
local key = KEYS[1]
local limit = tonumber(ARGV[1])
local window = tonumber(ARGV[2])
local now = tonumber(ARGV[3])
local start = now - window

redis.call('ZREMRANGEBYSCORE', key, 0, start)
local count = redis.call('ZCARD', key)
if count < limit then
    redis.call('ZADD', key, now, now .. ':' .. math.random())
    redis.call('EXPIRE', key, window)
    return 1
end
return 0
```

### 5.5 RocketMQ 削峰填谷

| 场景 | Topic | 说明 |
|------|-------|------|
| 视频转码 | `video-transcode` | 异步 FFmpeg 转码，生成 MP4 |
| 弹幕持久化 | `danmu-persist` | 批量写入 MySQL |
| 播放量统计 | `video-view` | 异步累加 Redis，定时同步 DB |
| 缓存同步 | `cache-sync` | Canal 触发，更新 Redis/ES |
| 审核通知 | `video-audit` | 视频审核结果通知 |

### 5.6 ElasticSearch 搜索

- **索引映射 (video_index)**:
  - 全文检索字段: `title` (ik_smart), `description` (ik_smart), `tags`
  - 聚合字段: `categoryId`, `status`
  - 排序字段: `viewCount`, `likeCount`, `createTime`
  - 建议字段: `title.suggest` (completion)

- **Canal 增量同步**: 监听 video 表变更 → 同步到 ES，延迟 < 1s
- **高亮显示**: 搜索匹配关键词在标题中高亮
- **聚合统计**: 分类下视频数量分布，时长区间分布

---

## 六、部署架构

```
                    ┌──────────────┐
                    │   Nginx 80   │
                    │  负载均衡    │
                    └──────┬───────┘
                           │
              ┌────────────┼────────────┐
              │            │            │
       ┌──────┴──────┐ ┌──┴──────┐ ┌──┴──────┐
       │ Bilibili-1  │ │ Bilibili│ │ Bilibili│
       │ :8801       │ │ -2:8802 │ │ -3:8803 │
       └──────┬──────┘ └──┬──────┘ └──┬──────┘
              │            │            │
              └────────────┼────────────┘
                           │
         ┌─────────────────┼─────────────────┐
         │                 │                 │
   ┌─────┴─────┐   ┌──────┴──────┐   ┌──────┴──────┐
   │  MySQL    │   │   Redis     │   │  RocketMQ   │
   │  :3306    │   │   :6379     │   │  :9876      │
   └─────┬─────┘   └──────┬──────┘   └──────┬──────┘
         │                │                  │
   ┌─────┴─────┐   ┌──────┴──────┐          │
   │  Canal    │   │     ES      │          │
   │  :11111   │   │   :9200     │          │
   └───────────┘   └─────────────┘          │
                                     ┌──────┴──────┐
                                     │    MinIO    │
                                     │   :9000     │
                                     └─────────────┘
```

- **Nginx**: 反向代理 + 静态资源 + IP 限流
- **Nacos**: 配置管理 + 服务注册中心
- **MySQL**: 主从架构，读写分离
- **Redis Cluster**: 哨兵模式高可用
- **ES Cluster**: 3 节点集群
- **MinIO**: 分布式对象存储 4 节点
- **RocketMQ**: 双主双从 Broker 集群
- **Canal**: 单节点监听 MySQL Master Binlog

---

## 七、中间件版本与端口

| 中间件 | 版本 | 端口 | 用途 |
|--------|------|------|------|
| MySQL | 8.0+ | 3306 | 关系型数据存储 |
| Redis | 7.x | 6379 | 缓存 / 限流 / 分布式锁 |
| ElasticSearch | 7.17+ | 9200/9300 | 全文检索 |
| RocketMQ | 5.x | 9876/10911 | 消息队列 |
| MinIO | RELEASE.2024 | 9000/9001 | 对象存储 |
| Canal | 1.1.7+ | 11111 | Binlog 同步 |
| Nacos | 2.x | 8848 | 服务治理 |
| Nginx | 1.25+ | 80/443 | 反向代理 |
